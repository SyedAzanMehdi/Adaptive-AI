node_modules/
dist/
build/
# TypeScript incremental build state — a committed tsbuildinfo makes `tsc -b`
# skip or misjudge work on a fresh CI checkout.
*.tsbuildinfo
# Secrets — never commit real env files (any depth), keep the .example templates
.env
.env.*
*.env
*.env.*
!.env.example
!**/.env.example
*.log
.DS_Store
mongo-data/
coverage/
~$*.docx
